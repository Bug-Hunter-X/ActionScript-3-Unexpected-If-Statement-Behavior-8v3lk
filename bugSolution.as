public function someMethod():void{

        //some code

        var someVariable:Boolean = someCondition; //Explicitly define the boolean variable
        if (someVariable) { //Use the boolean variable in the condition
            trace("Condition met");
        }
        //more code
        }